# Build output & dependencies
node_modules
dist
build
.next
coverage
*.tsbuildinfo

# Lockfiles & minified assets
pnpm-lock.yaml
*.min.js
*.min.css

# Generated native mobile projects — never hand-formatted
apps/mobile/android
apps/mobile/ios

# Framework-generated ambient declarations. Both are gitignored, so a fresh CI
# checkout never has them and `format:check` stayed green there — but Next and
# Expo write them the first time you run the app, and from then on every local
# `pnpm lint:all` fails on files nobody wrote and nobody may edit (Next rewrites
# next-env.d.ts on each build). CI isolation was hiding a local-only red.
apps/web/next-env.d.ts
apps/mobile/expo-env.d.ts

# Test fixtures / gold snapshots — reformatting can break snapshot comparisons
apps/api/gold-cases

# Script data & pipeline outputs (JSON data, not source)
apps/api/scripts/**/*.json

# Reports & local artifacts
e2e-report
apps/api/spend-gateway.json

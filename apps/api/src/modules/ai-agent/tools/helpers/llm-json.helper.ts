/**
 * LLM JSON Helper — re-exports from common/utils for backward compatibility.
 * Internal ai-agent consumers may continue importing from here.
 */
export { extractJsonFromLlm } from '../../../../common/utils/llm-json.util';

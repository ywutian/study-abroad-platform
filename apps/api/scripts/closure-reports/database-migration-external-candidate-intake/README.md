# Database Migration External Candidate Intake

Drop exact SQL recovery candidates for the unrecoverable DB-applied migration here.

## Target

- Migration: `20260428120000_add_mbti_and_personality_tags`
- Expected target path after operator approval: `apps/api/prisma/migrations/20260428120000_add_mbti_and_personality_tags/migration.sql`
- Required SHA-256: `cedbe8993cf570c0011ffcc3a5769445fe55c69285939873710d73bffeaffa66`
- Machine-readable target manifest: `TARGETS.json`
- Target manifest digest sidecar: `TARGETS.sha256`
- External exact-SQL request packet: `REQUEST.md` and `REQUEST.json`
- Current scan status: `STATUS.md` and `STATUS.json`

## Accepted Candidate Shapes

- `migration.sql`
- `20260428120000_add_mbti_and_personality_tags/migration.sql`
- A `.zip` containing `migration.sql` or `20260428120000_add_mbti_and_personality_tags/migration.sql`
- A `.tar`, `.tar.gz`, or `.tgz` containing `migration.sql`
- A `.sql.gz` containing the exact SQL bytes

## Verification

From the repository root, run:

```bash
pnpm --filter api audit:database-migration-external-artifact-packet -- --candidate-root apps/api/scripts/closure-reports/database-migration-external-candidate-intake --scan-archives
```

The candidate is usable only if the packet reports an exact SHA-256 artifact match. This intake is for read-only checksum verification only; do not copy files into `apps/api/prisma/migrations`, run Prisma write commands, or approve baseline fallback from this directory alone.

## Checklist

- Keep `TARGETS.json`, `TARGETS.sha256`, `REQUEST.md`, `REQUEST.json`, `STATUS.md`, and `STATUS.json` with the intake; all are regenerated by the read-only external artifact packet. The manifest lists the active target path, checksum, accepted artifact shapes, source report paths, and prohibited pre-verification actions, the digest sidecar lets the handoff confirm the target manifest did not drift, the request packet can be forwarded to deployment artifact, backup, CI artifact, or teammate-clone owners, and the status packet records the latest scan result after candidates are dropped, including how many non-generated candidate files are present in this directory.
- Keep external artifacts in this directory only until the read-only packet verifies them.
- Preserve original artifact packaging when possible; the verification command scans archive entries with `--scan-archives`.
- Treat any candidate without an exact SHA-256 match as rejected evidence.
- Do not restore, copy, resolve, deploy, or baseline from an unverified candidate.

2026-04-01 push verification limitation

- Runtime media exercised:
  - Android emulator for earlier Home / Profile / Prediction / Notifications runtime checks
  - USB-connected physical Android phone `PCT_AL10` with standalone Android dev build (`com.studyabroad.mobile`)
- On the physical device, the app auto-logged into `alice.zhang@demo.studyabroad.com`, rendered the real applicant session, and resolved the standalone `studyabroad://` scheme successfully.
- The remaining blocker is now native Android push configuration, not audit media. Metro and the on-device warning both reported:
  - `useNotifications: failed to register for push notifications`
  - `Default FirebaseApp is not initialized in this process com.studyabroad.mobile`
- Repo state confirms the missing native config:
  - `apps/mobile/android/app/google-services.json` is absent
  - `apps/mobile/android/app/build.gradle` and `apps/mobile/android/build.gradle` are now ready to apply Google Services when that file is supplied
- Result: true Expo push token issuance, remote push delivery, and notification-open behavior remain blocked until Android Firebase / FCM credentials are added and the dev build is rebuilt on the physical device.

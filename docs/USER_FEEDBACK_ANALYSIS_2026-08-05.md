# User Feedback Triage — 2026-08-05

> Filed per `docs/templates/feedback-triage.md`. **All triaged items are now
> closed in the current branch.** Historical fixes landed in #557, #558, #560,
> #562, #564–#568, #584 and #587; the final local verification and P4c layout
> closure were completed on 2026-08-11. See [Closure verification](#closure-verification-2026-08-11).
>
> **⚠️ REVISED after investigation (same day). The screenshots are stale.**
> Nine of the seventeen items were already fixed on `main` between 2026-07-01 and
> 2026-07-03 (#480, #483, #486, #489) and are live in production — verified by
> `git merge-base --is-ancestor` plus direct measurement of www.lumniedu.com, not
> by reading commit messages. The first draft of this file recorded several of
> them as `open` with invented root causes. That draft was wrong; this is the
> corrected version, and the correction is kept visible rather than silently
> overwritten.
>
> **What survived, and how each item was verified, is in
> [Resolution status](#resolution-status-verified-2026-08-05-end-of-day).**
> Root Cause is filled in only where it was traced to code AND, where possible,
> confirmed against production.

## Source Info

| Field             | Value                                               |
| ----------------- | --------------------------------------------------- |
| **Feedback from** | Product owner (self-review pass over the live site) |
| **Date received** | 2026-08-05                                          |
| **Channel**       | Annotated screenshot document, 8 sections           |
| **Total items**   | 17 actionable (2 sections passed clean)             |

**Sections reviewed:** 1 School Library ✅ · 2 Prediction · 3 Community Forum ·
4 Tindermatch · 5 Dashboard · 6 Case Library · 7 Timeline · 8 Personality Test ✅

---

## Closure verification (2026-08-11)

This pass closes the former `verify-only` rows against the current branch. It is
local acceptance evidence, not a claim that the uncommitted changes have already
been deployed to production.

- **P1–P4:** component/contract tests pin default-collapsed detail, an empty
  initial estimate list, disabled-button reasons, malformed preview-response
  recovery and the single-page-scroll layout. Browser acceptance in English and
  Chinese covers desktop/mobile/wide; desktop additionally proves empty → import
  list → submit `gpa: 95, gpaScale: 100` → visible preview results. The DTO suite
  covers every supported GPA scale, and the reach-suggestion test now requires
  at least four suggestions plus both named-program categories.
- **D0, D2–D5:** dashboard structure tests pin deadlines/timeline before the
  task queue, with Quick Ask and compact setup progress in the sidebar. The
  English/Chinese desktop and mobile dashboard surfaces pass the full UI crawler.
- **TL1:** the timeline introduction now explains that the feature turns saved
  schools, exams and personal events into dated next actions with deadline risk
  and progress. English/Chinese desktop and mobile surfaces pass the crawler.

P4c is no longer deferred: the selector and What-if panel remain sticky as one
normal-flow column, but the column no longer owns an inner vertical scrollbar.
At 1440×1000 and 2048×1200 the browser assertion finds zero vertically scrollable
descendants under `main`; the page scrollbar is the sole vertical scroll owner.

---

## Resolution status (verified 2026-08-05; refreshed 2026-08-06 after #564–#568, and 2026-08-07 after #587)

Everything below was traced to specific code. Nothing here is a guess — and the
`Verified by` column says which KIND of evidence, because "the tests pass" and
"production stopped doing it" are not the same claim.

| ID                             | What                                                              | Status                       | Verified by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------ | ----------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C1′**                        | `source:` provenance tags shipped to readers                      | ✅ FIXED (#557)              | **Production, measured**: `GET /cases` 20/20 → **0/20**, `GET /essay-ai/gallery` 10/10 → **0/10**. Sample tags now `['brown','shemmassian']`; `sourceUrl` still present, so the "查看原文" signal is intact                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **P4a**                        | 100-point / IB-45 / 6.0 GPA scales 400'd on every Preview         | ✅ FIXED (#558)              | Source on main: `@IsIn(GPA_SCALES_MUTABLE)`, `@Max(5)` gone. 14 cases pass through the real `plainToInstance` + `validateSync` path. **Not** re-verified in production (needs a logged-in profile on a non-4.0 scale)                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **P3**                         | Estimate list pre-filled itself on every visit                    | ✅ FIXED (#558)              | Source on main: prefill gated on `!wantsAutoRun`; `?autorun=1` coupling pinned by a contract test falsified three ways. **Not** re-verified in production                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **P4b**                        | Disabled Preview swallowed the click with no feedback             | ✅ FIXED (#558)              | Source on main: `disabledReason` + `profileBlockedHint`, one test asserts the INVARIANT (anything that disables must also give a reason). **Not** re-verified in production                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **P2**                         | Only 2 "Improve chances" suggestions on a reach school            | ✅ FIXED (#560)              | Source on main: two new branches surface the 46-entry catalogue; reach 2 → 4. Falsified by removing them. **Not** re-verified in production                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **P4c**                        | Results may render below COL1's inner scroll fold                 | ✅ FIXED (2026-08-11)        | Removed COL1's `max-height`/`overflow-y-auto`; selector + What-if remain one normal-flow sticky column. English/Chinese desktop/wide browser assertions find zero inner vertical scroll owners, and preview results render in the page flow                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **D5, TL1**                    | Timeline had nothing to show                                      | ✅ FIXED (#564, #565)        | `GlobalEvent` was an empty table, not a missing feature. 17 SAT/ACT/AP sittings seeded through `migrate.sh` (`global-events`, line 121); every record carries a `sourceUrl`. `lint:seed-freshness` reddens when the latest date falls inside 4 months                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **D2, D3, D4**                 | Dashboard reading order / Quick Ask / naming                      | ✅ FIXED (#566)              | Quick Ask moved to the sidebar; D3/D4 had already shipped 07-02/07-03 and only the tour bubble still pointed at the old anchors. `tour-anchors.test.ts` now fails if a tour targets a `data-tour` that no markup renders                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **D6-trending**                | Honest reading list (not fake "hot")                              | ✅ FIXED (#584)              | Component mounted; copy no longer claims heat/recency; empty state is honest. Real trending waits on organic replies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **F2**                         | Forum looked like a Reddit clone / empty cold start               | ✅ DECIDED + shipped         | Strip literal `r/` chrome; 0-post state is prompt-first (community starter chips → create). Not a full visual redesign. Relevance/cold-start > brand polish while the feed is empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **P2-tail**                    | Cross-school suggestion dedupe?                                   | ✅ DECIDED — keep per-school | Workbench is already overview (portfolio balance) → drill into one school; suggestions only render in the detail pane, so users never see N copies while scanning. Catalogue repetition across schools is intentional post-#560. No engine/UI cross-school layer.                                                                                                                                                                                                                                                                                                                                                                                                     |
| **T1**                         | Tindermatch's match pool was a browsable shell with nothing in it | ✅ FIXED (#587)              | **Production, measured** on `3bda384b`: pools **1 → 9**, entries **0 → 62**, none empty (热门主流比赛 8 · 数学 9 · 科研 8 · 编程与工程 8 · 商业案例 7 · 辩论与写作 6 · 艺术与人文 5 · 国际顶级 5 · 中国本土 6) — the same 62 the throwaway-DB run produced. Competition editions **3 → 14**: all 12 committed 2026-2027 records landed with `sourceUrl` + `verified: true` + `confidence: high`; the 2 pre-existing Presidential Scholar rows still carry no `sourceUrl`, correctly — they are synthetic leftovers and nothing overwrote them. Curl'd, not read off the deploy log: `migrate.sh` is fail-soft, so a seed that never ran still leaves the deploy green |
| `pending-decisions` season bug | Any past `decisionDate` marks a school "released"                 | ✅ FIXED (#568)              | The blocker named here is gone: `prediction-persistence` now stamps `applicationYear` at **all 3 write sites** from the single `resolveApplicationYear()`, so the join has a column to join on. `listPendingDecisions` pairs on `${schoolId}:${year}`. Rows written before the stamp are `null` = season unknown — **not offered, not backfilled** (backfilling means guessing a season)                                                                                                                                                                                                                                                                              |

⚠️ **The honest boundary**: only C1′ and T1 were re-measured against production.
P1–P4, D0/D2–D5 and TL1 now have local component/contract and/or logged-in
fixture-backed browser acceptance, but the 2026-08-11 working-tree changes are
not deployed. Treat them as "fixed and locally accepted", not as "observed
working in production".

T1 is the reason that distinction is worth keeping. Its fix ships through
`migrate.sh`, which is deliberately fail-soft: a seed that throws prints one
WARNING line and the deploy still goes green. "Deploy to GCP: success" would
have looked identical if none of the three seeds had run.

### Original finding detail

| ID                                       | What                                                                                 | Root cause                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Evidence                                                                                                                                                     |
| ---------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **P4**                                   | What-if Preview does nothing                                                         | Three independent defects — see the table                                                                                                                                                                                                                                                                                                                                                                                                                   | code, unverified in browser                                                                                                                                  |
| **P3**                                   | Prediction auto-selects the whole school list                                        | `prediction/page.tsx:129-133` prefill effect, untouched by #480                                                                                                                                                                                                                                                                                                                                                                                             | code                                                                                                                                                         |
| **P2**                                   | Only 2 "Improve chances" suggestions                                                 | **The engine, not the UI.** `prediction.service.ts:1716` force-enables counselor mode, so `:1902` returns early and the served path passes `aiSuggestions: undefined` (`:715`). Only 2 rule branches fire for a reach school; the other two need "zero activities/awards" or "no SAT/ACT". The 46-program catalog is never turned into suggestions — `:2843-2848` flattens it into ≤3 names interpolated inside an existing bullet.                         | code                                                                                                                                                         |
| **C1′**                                  | Provenance strings still shown to users — **on the case CARDS, not the detail page** | #489 fixed the detail page (`cases/[id]/page.tsx:244` now guards on `activityList`, which is null for every production case). It did **not** touch `case-card.tsx:155-159`, which renders raw `tags` as badges. The API ships tags because `case-query.service.ts:158-166` uses `include` (all scalars) and `stripCaseIdentity` only removes `userId`/`verifiedBy`. Same leak on mobile: `app/case/[id].tsx:203-212`, `screens/swipe/CaseCard.tsx:176-178`. | **CONFIRMED LIVE 2026-08-05: 20/20 cases on `GET /api/v1/cases` carry a `source:https://…` tag; sample `['source:…#essay14#anon', 'brown', 'shemmassian']`** |
| **D5, D6-trending, TL1, D2, D3, D4, F2** | Product/UX asks                                                                      | Not defects — need decisions                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                                                                                                                            |
| **T1**                                   | Was filed here as a product ask                                                      | **Reclassified 2026-08-07: it is a defect.** Three seeds were never wired into `migrate.sh`, so prod's match pool served `entries: []`. See the T1 row below.                                                                                                                                                                                                                                                                                               | —                                                                                                                                                            |

### Secondary defects found while investigating (not in the feedback)

| What                                                             | Root cause                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Every dialog is 512px wide, not the width its author asked for   | ✅ FIXED (#561). Callers now use `sm:max-w-*` so tw-merge competes in the same modifier scope; `no-unprefixed-dialog-width` in `apps/web/scripts/check-code-quality.ts` is error-level with a cleared worklist. Do not re-open this row as pending.                                                                                                               |
| `pending-decisions` treats any past `decisionDate` as "released" | ✅ FIXED (#568). Same blocker as the resolution-status row above: `prediction-persistence` stamps `applicationYear` at all 3 write sites via `resolveApplicationYear()`; `listPendingDecisions` pairs on `${schoolId}:${year}`. Pre-stamp rows stay `null` = season unknown — not offered, not backfilled. The 2026-08-05 "deliberately NOT fixed" note is stale. |
| The "school(s)" label is correct only by accident                | OPEN residual → G3.7. Unique identity is now `(profileId, schoolId, applicationYear)`. `listPendingDecisions` returns at most one row per `schoolId` (latest). Do not drop that distinct or the copy becomes a row count again.                                                                                                                                   |

---

## Triage Table

| ID      | Description (user's words, condensed)                                                                                                                                 | Category                                             | Root Cause                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                        | Batch | Status |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------ |
| **P1**  | 「稍微减少一点解释数据怎么被算出来的…现在这个占比太高了」(EVIDENCE USED + ADDITIONAL SIGNALS)                                                                         | `UX_CONFUSION`                                       | **Already fixed in #480** — collapsed into a default-collapsed `<details>`. Screenshot predates it or the details block is being expanded by default somewhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Landing on an estimate, the how-it-was-computed blocks are collapsed; one summary line visible; expanding is one click                                                                                                                                                                                                                                     | —     | `done` |
| **P2**  | 「improve chances 的地方需要变的更重要…可以多一点这种 suggestions」                                                                                                   | `UX_CONFUSION` + `DATA_ISSUE`                        | Placement fixed in #480 (moved above the technical explanation). **Count not addressed** — still 2 suggestions. `packages/shared/src/constants/prediction.ts` holds 46 programs by major category, so the catalog is not the limit. NEEDS INVESTIGATION: does the engine emit 2, or does the UI render only 2?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A reach school with a filled profile shows ≥4 distinct, actionable suggestions; at least 2 name a specific program matched to the user's major                                                                                                                                                                                                             | 2     | `done` |
| **P3**  | 「不要 automatically 把 school list 的学校搞到 search box 里面不然还得一个一个删了」— 应默认空 + "select from your school list" + "select all"                        | `CODE_BUG`                                           | `prediction/page.tsx:129-133` — a `useEffect` unconditionally pre-fills selection from the school list on every mount. #480 added `Import my list (N)` + `Clear` buttons but **left the prefill in place**, so the button reads as a re-import. ⚠️ `?autorun=1` depends on the prefill (`page.tsx:484` early-returns on `!hasPreFilled`); two entry points push it (`dashboard-setup-progress.tsx:56`, `onboarding/quick-experience.tsx:239`) and would **silently no-op** if the effect is simply deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Opening `/prediction` shows an empty estimate list; "Select from my school list" fills it on click; `/prediction?autorun=1` from dashboard AND from onboarding still auto-runs                                                                                                                                                                             | 1     | `done` |
| **P4**  | 「这个我好像用不了…我按这个是没有反应的」(What-if Preview)                                                                                                            | `CODE_BUG`                                           | **Three independent defects, none of which is what #480 fixed.** (1) `prediction-request.dto.ts:55-69` caps `gpa`/`gpaScale` at `@Max(5)` while `profile.dto.ts:76` allows `[4, 5, 6, 45, 100]` → any 100-point / IB-45 / 6.0-scale student gets a guaranteed 400 on every press. (2) The button is `disabled` when no schools are selected or `profileBlocksPrediction`, and `disabled:pointer-events-none` eats the click with zero feedback; gating a what-if simulator on `MISSING_GPA` is backwards since the user types a GPA into it, and the server does not enforce it. (3) The panel sits in COL1's `lg:max-h-[100dvh−header] lg:overflow-y-auto` box under the selector card — results likely render below that inner scroller's fold (**inferred from class names, not measured**). **Fully decomposed — every branch already has a verdict.** P4a: `prediction-request.dto.ts` now reads `@Max(GPA_MAX)` + `@IsIn(GPA_SCALES_MUTABLE)` off `packages/shared/src/constants/gpa.ts`, whose `GPA_SCALES = [4.0, 5.0, 6, 45, 100]` and whose docstring narrates this exact 400 (`@Max(5)` vs the profile's five scales). P4b: `PredictionWhatIfPanel.tsx:82-87,252-254` computes a `disabledReason` and renders it, with `whatIf.profileBlockedHint` in both locales. P4c: fixed 2026-08-11 by removing the inner vertical scroll owner; browser coverage pins page-scroll-only behavior. Nothing is left under this umbrella — `done` after the 2026-08-11 logged-in browser run submitted a 95/100 scenario, verified the request payload and rendered preview results in both locales. | A student on a 100-point scale can run a preview and see results; a disabled Preview states why; results are visible without hunting for an inner scrollbar                                                                                                                                                                                                | 1     | `done` |
| **F1**  | 「Forum 里点开一个 post 滑不了必须拉旁边的竖条」                                                                                                                      | `CODE_BUG`                                           | NEEDS INVESTIGATION — post detail modal appears to scroll only via its own inner scrollbar; wheel/trackpad over the body does nothing **Verified in production 2026-08-07**: opened the first post, moved the cursor to the CENTRE of the dialog (the body, not the scrollbar) and sent a real wheel event — the `flex-1 overflow-y-auto p-6` region went `scrollTop` 0 → 463 of 1003−540. Wheel over the body scrolls it, which is the acceptance verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Opening a forum post, scrolling with wheel/trackpad anywhere over the post body scrolls it                                                                                                                                                                                                                                                                 | 3     | `done` |
| **F2**  | 「在想会不会太抄袭 reddit 了哈哈哈哈」                                                                                                                                | `INDUSTRY_SUGGESTION`                                | Decided 2026-08-07: the real failure with an empty feed is cold-start, not brand plagiarism. Strip literal `r/` chrome; empty state offers community starter chips that open create-post. Full visual redesign deferred until there is organic activity to judge against.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Empty forum shows starter topics; no `r/` prefix on community names; create gated on login                                                                                                                                                                                                                                                                 | —     | `done` |
| **T1**  | 「你能上线一些 demo 池子比赛啥的东西吗，我来试用一下然后一起给你反馈」(Tindermatch)                                                                                   | `DATA_ISSUE`                                         | **Not "no demo content" — measured against prod 2026-08-07:** the deck already served 20 cards and `GET /teams` 20 teams. What was missing is upstream of that. Three seeds were dev-only, so `migrate.sh` never ran them: `seed-competitions` (112 reference `Competition` rows), `upsert-competition-data` (the committed 12-record 2026-2027 schedule JSON), and `seedMatchPools`, which lives inside the demo-and-destructive `seed-teams.ts` and so had no prod-safe entry point. Result: prod served **3** competition editions of 12, and its single MatchPool returned `entries: []` — a browsable pool with nothing in it. Same shape as the GlobalEvent empty-table gap (step 18), and it is why `check-seed-pipeline-parity.ts` exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `GET /teams/match-pools/:id` returns non-empty `entries` for every pool, and `GET /teams/competition-editions` serves the 12 sourced 2026-2027 editions — verified on prod, not just in the seed log. **Met 2026-08-07 on `3bda384b`: 9/9 pools non-empty (62 entries), 14 editions of which the 12 committed ones carry `sourceUrl` + `verified: true`.** | 4     | `done` |
| **D0**  | 「需要幻想一下作为一个用户我每次打开这个网站到底想要看到什么」— wants: (1) immediate deadlines + progress (2) profile completion (3) what's going on in the community | `UX_CONFUSION`                                       | **This is the frame for D1–D6, not a separate item.** The dashboard currently leads with an outcome-reporting banner and an AI ask box; the user's stated top-three is deadlines → profile completion → community. **Order verified in the render tree** (`dashboard/page.tsx`, 2026-08-07): `DashboardEventsTimeline` is the FIRST main-column surface (`:316`), `DashboardTrending` is present (`:349`), and `DashboardSetupProgress` sits well below at `:399` — deadlines → … → profile completion, which is the stated priority. The comment at `:304` records the reorder as deliberate (#566). `done`, not `done`: the acceptance also says profile completion should be **compact**, and visual weight is not something the JSX order can settle — that needs eyes on a real logged-in dashboard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Dashboard's visual order matches the stated priority: timeline/deadlines first, profile completion secondary and compact, community/trending present                                                                                                                                                                                                       | 5     | `done` |
| **D1**  | 「写着 50 个学校在等我的 outcome…可是 school list 里面其实只有十几个学校…对我来说没有什么意义」                                                                       | `CODE_BUG`                                           | `outcome-pending-banner.tsx:56` renders `t('bannerTitle', { count: pending.length })` where `pending` = `GET /outcomes/pending-decisions`, i.e. **prediction rows**, not distinct schools. Snapshot shows PREDICTIONS = 14 and the school list ~a dozen, so 50 rows implies repeat predictions on the same schools each leaving a row. Copy says "school(s)", which is what makes it read wrong. **Hypothesis — not yet confirmed against the DB.** **Already fixed — read off main 2026-08-07, and the DB guarantees it.** `PredictionResult` unique identity is now `(profileId, schoolId, applicationYear)` (G3.7, 2026-08-17). `listPendingDecisions` returns at most one row per `schoolId` (latest). Do not treat `pending.length` as distinct-school without that distinct — two seasons can coexist. Residual: migration must be applied before production DB matches the schema. `listPendingDecisions` additionally narrows to `SchoolListItem`, and the comment there names this exact defect: "Without this the banner counted every PredictionResult row (50+) instead of the ~14 saved schools". The label says school(s), which is what is counted. Residual, not a defect: the query is `take: 50`, so a user with >50 saved schools would see a capped number.                                                                                                                                                                                                                                                                                                                    | The banner's number equals the number of distinct schools genuinely awaiting an outcome, and the label matches whatever is being counted                                                                                                                                                                                                                   | 5     | `done` |
| **D2**  | 「这个问题框可以变小一点放在屏幕的旁边…我不想要 ai 答问变成首页进来的一个重点」                                                                                       | `UX_CONFUSION`                                       | Layout/priority decision — the AI ask box currently occupies prime above-the-fold width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | The AI ask box is a secondary, side-placed affordance; it is not the first thing in the dashboard's reading order                                                                                                                                                                                                                                          | 5     | `done` |
| **D3**  | 「这个进度条我觉得用处没有太大…变成小一点在屏幕旁边的一个进度完善程度展示会更好」(Build a balanced school list setup checklist)                                       | `UX_CONFUSION`                                       | Same class as D2 — the setup checklist is full-width and high in the page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Setup progress is a compact side element, still visible but not occupying a full-width block                                                                                                                                                                                                                                                               | 5     | `done` |
| **D4**  | 「priority queue 比较好因为用处很大，最好改名成 "Approaching Deadlines" 或 "to-do list"…希望展示比较详细的任务」                                                      | `UX_CONFUSION`                                       | Naming + task detail. The feature is wanted; the label is opaque and the items are too coarse.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | The section is named in plain language (e.g. "Approaching Deadlines"), and each row states a concrete action with its due date                                                                                                                                                                                                                             | 6     | `done` |
| **D5**  | 「deadline stream 没有太大用处，可以改成大体的 timeline overview…让人清楚知道 "ok 这周得提交 columbia，下周需要提交托福"…放在 deadlines 前面」                        | `NEW_FEATURE`                                        | The current deadline stream is a flat activity feed. What is wanted is a calendar-shaped overview (which school closes when, which exam on which date), placed ABOVE D4's task list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Dashboard shows a timeline overview of upcoming school deadlines / exams / competitions, above the task list; a user can read their next 2–3 weeks at a glance                                                                                                                                                                                             | 6     | `done` |
| **D6**  | 「这个不要收起来，自然就展开的就好了」(Snapshot card) + 「希望有一个 "trending" 的板块…今天热搜的是什么…论坛里面有什么火的讨论…新发布的非常好的文章」                 | `UX_CONFUSION` (collapse) + `NEW_FEATURE` (trending) | Two separate asks filed together: Snapshot defaults to collapsed; and there is no trending/community-activity surface on the dashboard. **Both halves already closed.** Snapshot: `dashboard/page.tsx:423` renders `<DashboardStats defaultOpen={!isEmptyOnboarding} />`, i.e. expanded by default everywhere except the empty-onboarding state — the component's own doc comment records the change. Trending: shipped in #584 (honest reading list; real trending waits on organic replies) and already ✅ in the resolution table above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Snapshot renders expanded by default; a trending section shows current hot forum discussions / new articles                                                                                                                                                                                                                                                | 7     | `done` |
| **C1**  | 「activities&highlight 应该是他们申请的活动之类的，这里都是写的这个文书是从哪里来的」                                                                                 | `DATA_ISSUE`                                         | The "Activities & Highlights" block is rendering source-attribution values (`source:https://…shemmassianconsulting.com/…`, `washu`, `shemmassian`) instead of the applicant's activities. NEEDS INVESTIGATION: whether the ingestion wrote provenance into the activities field, or the card is reading the wrong field. **Either way the label is making a provenance string look like applicant data — that is a credibility problem, not a cosmetic one.** **Verified in production 2026-08-07** (headless Chromium, `/zh/cases/cmpdysq3i…`): zero URLs anywhere in the rendered page and no activities/highlights heading at all — the block is hidden, which is the second half of the acceptance. `activityList` is null on all 20 public cases, and the `source:` tags C1′ removed have not come back (`tags: ["brown","shemmassian"]`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | A case detail shows real applicant activities under that heading, or the block is hidden when no activity data exists — never source URLs                                                                                                                                                                                                                  | 3     | `done` |
| **C2**  | 「这个标题被挡住啦！稍微改一下就好啦！」(case card titles truncated)                                                                                                  | `CODE_BUG`                                           | NEEDS INVESTIGATION — case card title clipped. Likely the `min-w-0` / `truncate` class from the overflow rules (`.claude/rules/frontend.md` Layer 3) missing on the title's flex parent. **Verified in production 2026-08-07**: the card title is the school-name `<h3 className="truncate …">` in `case-card.tsx:88`, which already carries the #214/#217 fix in a comment. Measured at 1280 / 768 / 390 px — 12 titles sampled per width, `scrollWidth - clientWidth = 0` on every one, `text-overflow: ellipsis`. Nothing cut mid-glyph, nothing hidden behind a sibling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Case card titles are readable at all supported widths — truncated with an ellipsis at worst, never cut mid-glyph or hidden behind a sibling                                                                                                                                                                                                                | 3     | `done` |
| **C3**  | 「这个 winning essays 没太懂，win 了啥？」                                                                                                                            | `UX_CONFUSION`                                       | Label is ambiguous — "Winning" reads as a contest win rather than "essays from admitted applicants". **The label is gone.** `Winning` appears nowhere in `apps/web/src`, `apps/api/src` or `packages/shared/src` as of 2026-08-07. The surfaces now read `Essay Gallery` / `Sample Essays` / `Admitted Essays` — which say what they are without the contest reading. No naming decision is outstanding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | The section's name and its one-line description make it unambiguous what the essays are and where they came from                                                                                                                                                                                                                                           | 3     | `done` |
| **TL1** | 「这里不太确定是啥」(Schools Awaiting Timeline + Generate Timeline buttons)                                                                                           | `UX_CONFUSION`                                       | The screen presents a bare list of schools each with a "Generate Timeline" button and no explanation of what a timeline is, why it needs generating, or what happens after.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A first-time visitor to /timeline can tell what the page produces and why, without clicking to find out                                                                                                                                                                                                                                                    | 7     | `done` |

---

## Batches

| Batch | Items          | Theme                                | Why grouped                                                                                                                            |
| ----- | -------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | P3, P4         | Prediction — the two real defects    | Both are `CODE_BUG` in the same page; P4's DTO cap is a one-line server fix, P3 is one effect + a guard. Highest value, smallest diff. |
| **2** | P2             | Prediction — suggestion count        | Needs a decision on engine-vs-UI first; isolated so it can't block batch 1.                                                            |
| **3** | F1, C1, C2, C3 | Forum + Case Library defects         | Independent, all small, all user-visible correctness/clarity. C1 carries the credibility risk.                                         |
| **4** | T1             | Tindermatch demo data                | Data/seed only, no product decisions, unblocks a whole feedback pass.                                                                  |
| **5** | D0, D1, D2, D3 | Dashboard — above-the-fold reorder   | D0 is the frame; D1's wrong number and D2/D3's over-weighted blocks are all "what dominates the first screen". One coherent change.    |
| **6** | D4, D5         | Dashboard — deadlines & timeline     | D5 must land before/with D4 since the user wants the overview placed above the task list.                                              |
| **7** | D6, TL1        | Community surface + timeline clarity | Both are "explain/expose what's going on"; D6's trending and TL1's purpose copy are adjacent.                                          |

**Sequencing:** 1 → 3 → 4 in any order (all defects, no decisions needed). 2, 5, 6, 7
need a product decision first — see below.

---

## Needs a decision before coding

| Item              | The question                                                                                                                                                                                                                                                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~**F2**~~        | **Answered 2026-08-07.** Cold-start > brand polish while the feed is empty: strip literal `r/` chrome; 0-post state is prompt-first (community starter chips → create). Not a full Reddit visual redesign — that waits on organic activity.                                                                                      |
| ~~**P2** (tail)~~ | **Answered 2026-08-07 — keep per-school.** Count already fixed (#560). Workbench reading model is portfolio overview → drill into one school; suggestions only live in the detail pane, so cross-school repetition is not visible while scanning. Cross-school ranking/dedupe would risk wrong early-round tips. No extra layer. |
| ~~**D0/D2/D3**~~  | **Answered by shipping it** (#566): Quick Ask sits in the sidebar, deadlines lead. Reopen if the order still reads wrong.                                                                                                                                                                                                        |
| ~~**D5**~~        | **Answered by shipping it.** Scope = school deadlines + standardized tests; competitions already had their own pipeline. The blocker was never the decision — the table was empty.                                                                                                                                               |
| ~~**D6**~~        | **Answered by shipping the honest reading list (#584).** Do not feed "trending" from seed likes/views (random) or manufacture heat. Restore real trending only when the forum has organic replies to rank.                                                                                                                       |

---

## Already fixed before this feedback was written

All four commits verified on `origin/main` with `git merge-base --is-ancestor`.

| ID                                   | Fixed by        | Date  | Verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------ | --------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P1** explanation blocks collapsed  | `3b3e65dd` #480 | 07-01 | `PredictionDetailPane.tsx:479` — `<details>` with no `open`, no state, no JS toggle; the only two render sites of both labels are inside it. **But the parent "how this estimate was derived" panel (`:434-475`) is still unconditionally expanded with four uppercase labels — that is probably what still looks heavy.**                                                                                                                                                                                                                                                                                                                                                   |
| **F1** forum post won't wheel-scroll | `a4f0d31c` #483 | 07-02 | Cause was the global Lenis provider `preventDefault()`ing every wheel event. Fix adds `data-lenis-prevent` to `DialogContent` (`dialog.tsx:62-67`); lenis@1.3.17 honours it at `lenis.mjs:629`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **D1** "50 school(s) waiting"        | `080f5439` #486 | 07-02 | **The hypothesis in this file's first draft was wrong.** Rows were never duplicated under the then-unique `(profileId, schoolId)`. The real cause: the query had no school-list filter, so it counted PREVIEW rows that `getAIRecommendations` bulk-creates for **up to 100** schools the user never saved — and `take: 50` clipped it, so **"50" was the LIMIT, not a count**. Pinned by `outcome.service.spec.ts:381-390`. Post-fix the banner is mathematically ≤ the snapshot's PREDICTIONS=14, so 50-beside-14 proves the screenshot is old. **2026-08-17:** unique is now `(profileId, schoolId, applicationYear)` (G3.7); `listPendingDecisions` distincts by school. |
| **C2** card titles clipped           | `891ac998` #489 | 07-03 | Old markup had `flex items-center gap-2` with **no `min-w-0`** and a `shrink-0` RankingBadge reading "US News National Universities #24" — the badge took the row, leaving ~4 chars. Now `min-w-0 flex-1` + `truncate`. Measured live at 1280px: all 8 cards `clientWidth === scrollWidth`, zero truncation.                                                                                                                                                                                                                                                                                                                                                                 |
| **C3** "Winning Essays"              | `891ac998` #489 | 07-03 | en `"Sample Essays"`, zh `"范文"`. **Verified live: `curl /zh/cases` returns 范文 ×20, zero 优秀文书.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **C1** detail-page activities leak   | `891ac998` #489 | 07-03 | Detail page now guards on `activityList` only. ⚠️ **The card leak was NOT fixed — see C1′ above.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

**For C3, if you still want a better label:** the section is 190 essays, **190/190 admitted**, 176 to Top-20, harvested from public archives (shemmassian, collegeessayguy, collegevine, medium, the Crimson). So "范文" is accurate; what answers 「win 了啥」 is a one-line description saying _admitted students, republished from public archives_ — there is currently no per-tab description at all.

---

## Prior round: what #480 actually did

Commit `3b3e65dd` (#480, 2026-07-01, on `main`) addressed this **same four-item
Prediction list**. Outcome, checked against current `main`:

| Item | #480's claim                                                    | Current reality                                               |
| ---- | --------------------------------------------------------------- | ------------------------------------------------------------- |
| P1   | Collapse the explanation blocks into `<details>`                | ✅ done                                                       |
| P2   | Surface "Improve chances" above the technical explanation       | ⚠️ placement done, count untouched                            |
| P3   | Added `Import my list` / `Clear` buttons                        | ❌ prefill effect never changed — the complaint is unaffected |
| P4   | "0-result run shows an explicit empty state" + `scrollIntoView` | ❌ the cause was never a 0-result run                         |

**The lesson is the one already written into the triage template's own example:**
#480 diagnosed P4 as "a silent zero-result run" and fixed that. None of the three
actual defects (a 400 from a DTO cap, a dead disabled button, a nested scroll
container) is a zero-result run. It shipped **with no test**, so nothing
contradicted the claim, and the complaint returned verbatim five weeks later.

⇒ **Every item in this round lands with one check that fails if the item
regresses** — for P3 specifically, a test that `?autorun=1` still fires, because
that coupling breaks silently.

---

## Passed clean

- **1. School Library** ✅
- **8. Personality Test** ✅

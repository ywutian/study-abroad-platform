# 静态可视化

## 录取预测模型

在浏览器中打开（本地文件或静态托管均可）：

- [prediction-model.html](./prediction-model.html)

**说明**：单文件 HTML，含 Mermaid 流程图、权重条形图、融合/轮次/区域表格，以及可选的交互滑块（脚本逻辑须与 `packages/shared/src/scoring/score.ts` 保持同步）。
